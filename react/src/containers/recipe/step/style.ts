import styled from 'styled-components';

export const StepDescriptionInput = styled.textarea`
    resize: none;
    flex-grow: 40;
    margin-left: 8px;
    border-color: grey;
    border-radius: 5px;
`;

export const StepContainer = styled.div`
    display: flex;

    align-items: center;
    padding: 4px 0;
`;
